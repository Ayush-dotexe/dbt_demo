sources:
  - name: db
    database: workspace
    schema: default
    tables:
      - name: employees_table
        config:
          group: Dev
        columns:
          - name: join_date
            tests:
              - is_today

      - name: products  

